rabbitMq {
  vhost = "/"
  servers = [localhost]
  backoffDurationOnError = 500 ms
  connectTimeout = 0
  networkRecoveryInterval = 5000
  username = user
  password = password
}

marathon {
  url = "http://localhost:8080"
}

applications = [
  {
    name = "testapp1"
    queue = "test-queue"
    maxMessagesCount = 5,
    maxInstancesCount = 30
  }
  {
    name = "testapp2"
    queue = "test-queue2"
    maxMessagesCount = 5
  }
]